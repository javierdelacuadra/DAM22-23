<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".timber.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Formulario"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".ui.splashscreen.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <activity
            android:name=".ui.loginactivity.LoginActivity"
            android:parentActivityName=".ui.splashscreen.SplashActivity" />

        <activity
            android:name=".ui.usuarioactivity.UsuarioActivity"
            android:parentActivityName=".ui.loginactivity.LoginActivity" />

        <activity
            android:name=".ui.doctoractivity.DoctorActivity"
            android:parentActivityName=".ui.loginactivity.LoginActivity" />

        <activity
            android:name=".ui.listactivity.ListActivity"
            android:parentActivityName=".ui.listactivity.ListActivity"
            android:theme="@style/Theme.Formulario.NoActionBar" />

        <activity
            android:name=".ui.updateactivity.UpdateActivity"
            android:parentActivityName=".ui.listactivity.ListActivity" />

        <activity
            android:name=".ui.addactivity.AddActivity"
            android:parentActivityName=".ui.listactivity.ListActivity" />

    </application>

</manifest>