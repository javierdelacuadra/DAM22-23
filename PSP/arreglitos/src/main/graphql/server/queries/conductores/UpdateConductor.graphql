mutation UpdateConductor($id: Int!, $nombre: String!, $telefono: String!, $IDCamion: Int!) {
    actualizarConductor(id: $id, nombre: $nombre, telefono: $telefono, camionId: $IDCamion) {
        id
        nombre
        telefono
        camion {
            id
            modelo
        }
    }
}